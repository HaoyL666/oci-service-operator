---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: agentendpoints.generativeaiagent.oracle.com
spec:
  group: generativeaiagent.oracle.com
  names:
    kind: AgentEndpoint
    listKind: AgentEndpointList
    plural: agentendpoints
    singular: agentendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the AgentEndpoint
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the AgentEndpoint
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: AgentEndpoint is the Schema for the agentendpoints API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentEndpointSpec defines the desired state of AgentEndpoint.
            properties:
              agentId:
                description: The OCID of the agent that this endpoint is associated
                  with.
                type: string
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment to create the endpoint in.
                type: string
              contentModerationConfig:
                description: AgentEndpointContentModerationConfig defines nested fields
                  for AgentEndpoint.ContentModerationConfig.
                properties:
                  shouldEnableOnInput:
                    description: A flag to enable or disable content moderation on
                      input.
                    type: boolean
                  shouldEnableOnOutput:
                    description: A flag to enable or disable content moderation on
                      output.
                    type: boolean
                type: object
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: An optional description of the endpoint.
                type: string
              displayName:
                description: A user-friendly name. Does not have to be unique, and
                  it's changeable. Avoid entering confidential information.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              guardrailConfig:
                description: AgentEndpointGuardrailConfig defines nested fields for
                  AgentEndpoint.GuardrailConfig.
                properties:
                  contentModerationConfig:
                    description: AgentEndpointGuardrailConfigContentModerationConfig
                      defines nested fields for AgentEndpoint.GuardrailConfig.ContentModerationConfig.
                    properties:
                      inputGuardrailMode:
                        description: An input guardrail mode for content moderation.
                        type: string
                      outputGuardrailMode:
                        description: An output guardrail mode for content moderation.
                        type: string
                    type: object
                  personallyIdentifiableInformationConfig:
                    description: AgentEndpointGuardrailConfigPersonallyIdentifiableInformationConfig
                      defines nested fields for AgentEndpoint.GuardrailConfig.PersonallyIdentifiableInformationConfig.
                    properties:
                      inputGuardrailMode:
                        description: An input guardrail mode for personally identifiable
                          information.
                        type: string
                      outputGuardrailMode:
                        description: An output guardrail mode for personally identifiable
                          information.
                        type: string
                    type: object
                  promptInjectionConfig:
                    description: AgentEndpointGuardrailConfigPromptInjectionConfig
                      defines nested fields for AgentEndpoint.GuardrailConfig.PromptInjectionConfig.
                    properties:
                      inputGuardrailMode:
                        description: An input guardrail mode for prompt injection.
                        type: string
                    type: object
                type: object
              humanInputConfig:
                description: AgentEndpointHumanInputConfig defines nested fields for
                  AgentEndpoint.HumanInputConfig.
                properties:
                  shouldEnableHumanInput:
                    description: The Agent will request for human input for disambiguation
                      or additional information gathering if this is enabled.
                    type: boolean
                required:
                - shouldEnableHumanInput
                type: object
              metadata:
                additionalProperties:
                  type: string
                description: Key-value pairs to allow additional configurations.
                type: object
              outputConfig:
                description: AgentEndpointOutputConfig defines nested fields for AgentEndpoint.OutputConfig.
                properties:
                  outputLocation:
                    description: AgentEndpointOutputConfigOutputLocation defines nested
                      fields for AgentEndpoint.OutputConfig.OutputLocation.
                    properties:
                      bucketName:
                        description: The name of the bucket.
                        type: string
                      jsonData:
                        type: string
                      namespaceName:
                        description: The namespace of the object storage.
                        type: string
                      outputLocationType:
                        type: string
                      prefix:
                        description: The prefix of the object storage.
                        type: string
                    type: object
                  retentionPeriodInMinutes:
                    description: Retention duration of the output data.
                    type: integer
                required:
                - outputLocation
                type: object
              provisionedCapacityConfig:
                description: AgentEndpointProvisionedCapacityConfig defines nested
                  fields for AgentEndpoint.ProvisionedCapacityConfig.
                properties:
                  platformRuntimeConfig:
                    description: AgentEndpointProvisionedCapacityConfigPlatformRuntimeConfig
                      defines nested fields for AgentEndpoint.ProvisionedCapacityConfig.PlatformRuntimeConfig.
                    properties:
                      platformRuntimeConfigType:
                        description: The type of Platform runtime config.
                        type: string
                      version:
                        description: The version of the Core. The latest version will
                          be displayed as default.
                        type: string
                    type: object
                  provisionedCapacityId:
                    description: An OCID that uniquely identifies an Provisioned Capacity.
                    type: string
                  toolRuntimeConfigs:
                    description: RAG and SQL will be tools.
                    items:
                      description: AgentEndpointProvisionedCapacityConfigToolRuntimeConfig
                        defines nested fields for AgentEndpoint.ProvisionedCapacityConfig.ToolRuntimeConfig.
                      properties:
                        toolRuntimeConfigType:
                          description: The type of the tool.
                          type: string
                        version:
                          description: The version of the components.
                          type: string
                      required:
                      - toolRuntimeConfigType
                      type: object
                    type: array
                required:
                - provisionedCapacityId
                type: object
              sessionConfig:
                description: AgentEndpointSessionConfig defines nested fields for
                  AgentEndpoint.SessionConfig.
                properties:
                  idleTimeoutInSeconds:
                    description: The session will become inactive after this timeout.
                    type: integer
                type: object
              shouldEnableCitation:
                description: Whether to show citations in the chat result.
                type: boolean
              shouldEnableMultiLanguage:
                description: Whether to enable multi-language for chat.
                type: boolean
              shouldEnableSession:
                description: Whether or not to enable Session-based chat.
                type: boolean
              shouldEnableTrace:
                description: Whether to show traces in the chat result.
                type: boolean
            required:
            - agentId
            - compartmentId
            type: object
          status:
            description: AgentEndpointStatus defines the observed state of AgentEndpoint.
            properties:
              agentId:
                description: The OCID of the agent that this endpoint is associated
                  with.
                type: string
              compartmentId:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the compartment.
                type: string
              contentModerationConfig:
                description: AgentEndpointContentModerationConfig defines nested fields
                  for AgentEndpoint.ContentModerationConfig.
                properties:
                  shouldEnableOnInput:
                    description: A flag to enable or disable content moderation on
                      input.
                    type: boolean
                  shouldEnableOnOutput:
                    description: A flag to enable or disable content moderation on
                      output.
                    type: boolean
                type: object
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              description:
                description: An optional description of the endpoint.
                type: string
              displayName:
                description: A user-friendly name. Does not have to be unique, and
                  it's changeable.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Free-form tags for this resource. Each tag is a simple key-value pair with no predefined name, type, or namespace.
                  For more information, see Resource Tags (https://docs.oracle.com/iaas/Content/General/Concepts/resourcetags.htm).
                  Example: `{"Department": "Finance"}`
                type: object
              guardrailConfig:
                description: AgentEndpointGuardrailConfig defines nested fields for
                  AgentEndpoint.GuardrailConfig.
                properties:
                  contentModerationConfig:
                    description: AgentEndpointGuardrailConfigContentModerationConfig
                      defines nested fields for AgentEndpoint.GuardrailConfig.ContentModerationConfig.
                    properties:
                      inputGuardrailMode:
                        description: An input guardrail mode for content moderation.
                        type: string
                      outputGuardrailMode:
                        description: An output guardrail mode for content moderation.
                        type: string
                    type: object
                  personallyIdentifiableInformationConfig:
                    description: AgentEndpointGuardrailConfigPersonallyIdentifiableInformationConfig
                      defines nested fields for AgentEndpoint.GuardrailConfig.PersonallyIdentifiableInformationConfig.
                    properties:
                      inputGuardrailMode:
                        description: An input guardrail mode for personally identifiable
                          information.
                        type: string
                      outputGuardrailMode:
                        description: An output guardrail mode for personally identifiable
                          information.
                        type: string
                    type: object
                  promptInjectionConfig:
                    description: AgentEndpointGuardrailConfigPromptInjectionConfig
                      defines nested fields for AgentEndpoint.GuardrailConfig.PromptInjectionConfig.
                    properties:
                      inputGuardrailMode:
                        description: An input guardrail mode for prompt injection.
                        type: string
                    type: object
                type: object
              humanInputConfig:
                description: AgentEndpointHumanInputConfig defines nested fields for
                  AgentEndpoint.HumanInputConfig.
                properties:
                  shouldEnableHumanInput:
                    description: The Agent will request for human input for disambiguation
                      or additional information gathering if this is enabled.
                    type: boolean
                required:
                - shouldEnableHumanInput
                type: object
              id:
                description: The OCID (https://docs.oracle.com/iaas/Content/General/Concepts/identifiers.htm)
                  of the endpoint.
                type: string
              lifecycleDetails:
                description: |-
                  A message that describes the current state of the endpoint in more detail. For example,
                  can be used to provide actionable information for a resource in the Failed state.
                type: string
              lifecycleState:
                description: The current state of the endpoint.
                type: string
              metadata:
                additionalProperties:
                  type: string
                description: Key-value pairs to allow additional configurations.
                type: object
              outputConfig:
                description: AgentEndpointOutputConfig defines nested fields for AgentEndpoint.OutputConfig.
                properties:
                  outputLocation:
                    description: AgentEndpointOutputConfigOutputLocation defines nested
                      fields for AgentEndpoint.OutputConfig.OutputLocation.
                    properties:
                      bucketName:
                        description: The name of the bucket.
                        type: string
                      jsonData:
                        type: string
                      namespaceName:
                        description: The namespace of the object storage.
                        type: string
                      outputLocationType:
                        type: string
                      prefix:
                        description: The prefix of the object storage.
                        type: string
                    type: object
                  retentionPeriodInMinutes:
                    description: Retention duration of the output data.
                    type: integer
                required:
                - outputLocation
                type: object
              provisionedCapacityConfig:
                description: AgentEndpointProvisionedCapacityConfig defines nested
                  fields for AgentEndpoint.ProvisionedCapacityConfig.
                properties:
                  platformRuntimeConfig:
                    description: AgentEndpointProvisionedCapacityConfigPlatformRuntimeConfig
                      defines nested fields for AgentEndpoint.ProvisionedCapacityConfig.PlatformRuntimeConfig.
                    properties:
                      platformRuntimeConfigType:
                        description: The type of Platform runtime config.
                        type: string
                      version:
                        description: The version of the Core. The latest version will
                          be displayed as default.
                        type: string
                    type: object
                  provisionedCapacityId:
                    description: An OCID that uniquely identifies an Provisioned Capacity.
                    type: string
                  toolRuntimeConfigs:
                    description: RAG and SQL will be tools.
                    items:
                      description: AgentEndpointProvisionedCapacityConfigToolRuntimeConfig
                        defines nested fields for AgentEndpoint.ProvisionedCapacityConfig.ToolRuntimeConfig.
                      properties:
                        toolRuntimeConfigType:
                          description: The type of the tool.
                          type: string
                        version:
                          description: The version of the components.
                          type: string
                      required:
                      - toolRuntimeConfigType
                      type: object
                    type: array
                required:
                - provisionedCapacityId
                type: object
              sessionConfig:
                description: AgentEndpointSessionConfig defines nested fields for
                  AgentEndpoint.SessionConfig.
                properties:
                  idleTimeoutInSeconds:
                    description: The session will become inactive after this timeout.
                    type: integer
                type: object
              shouldEnableCitation:
                description: Whether to show citations in the chat result.
                type: boolean
              shouldEnableMultiLanguage:
                description: Whether to enable multi-language for chat.
                type: boolean
              shouldEnableSession:
                description: Whether or not to enable Session-based chat.
                type: boolean
              shouldEnableTrace:
                description: Whether to show traces in the chat result.
                type: boolean
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  System tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the AgentEndpoint was created, in the format defined by RFC 3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the endpoint was updated, in the format defined by RFC 3339 (https://tools.ietf.org/html/rfc3339).
                  Example: `2016-08-25T21:10:29.600Z`
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
