---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: drplans.disasterrecovery.oracle.com
spec:
  group: disasterrecovery.oracle.com
  names:
    kind: DrPlan
    listKind: DrPlanList
    plural: drplans
    singular: drplan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the DrPlan
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the DrPlan
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: DrPlan is the Schema for the drplans API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DrPlanSpec defines the desired state of DrPlan.
            properties:
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  The display name of the DR plan being created.
                  Example: `EBS Switchover PHX to IAD`
                type: string
              drProtectionGroupId:
                description: |-
                  The OCID of the DR protection group to which this DR plan belongs.
                  Example: `ocid1.drprotectiongroup.oc1..uniqueID`
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"Department": "Finance"}`
                type: object
              planGroups:
                description: An ordered list of groups in a DR plan.
                items:
                  description: DrPlanPlanGroup defines nested fields for DrPlan.PlanGroup.
                  properties:
                    displayName:
                      description: |-
                        The display name of the group.
                        Example: `My_GROUP_3 - EBS Start`
                      type: string
                    id:
                      description: |-
                        The unique id of the group. Must not be modified by user.
                        Example: `sgid1.group..uniqueID`
                      type: string
                    isPauseEnabled:
                      description: |-
                        A flag indicating whether this group should be enabled for execution.
                        This flag is only applicable to the `USER_DEFINED_PAUSE` group. The flag should be null for the remaining group types.
                        Example: `true`
                      type: boolean
                    steps:
                      description: The list of steps in this group.
                      items:
                        description: DrPlanPlanGroupStep defines nested fields for
                          DrPlan.PlanGroup.Step.
                        properties:
                          displayName:
                            description: |-
                              The display name of the step in a group.
                              Example: `My_STEP_3A - EBS Start - STAGE A`
                            type: string
                          errorMode:
                            description: |-
                              The error mode for this step.
                              The default error mode for the step is `STOP_ON_ERROR`.
                            type: string
                          id:
                            description: |-
                              The unique id of the step.
                              Example: `sgid1.step..uniqueID`
                            type: string
                          isEnabled:
                            description: |-
                              A flag indicating whether this step should be enabled for execution.
                              The default value for the isEnabled flag is `true`.
                              Example: `true`
                            type: boolean
                          timeout:
                            description: |-
                              The timeout in seconds for executing this step.
                              When creating a new step, if no timeout is specified, the default timeout is set to `3600` seconds.
                              Example: `600`
                            type: integer
                          userDefinedStep:
                            description: DrPlanPlanGroupStepUserDefinedStep defines
                              nested fields for DrPlan.PlanGroup.Step.UserDefinedStep.
                            properties:
                              functionId:
                                description: |-
                                  The OCID of function to be invoked.
                                  Example: `ocid1.fnfunc.oc1..uniqueID`
                                type: string
                              jsonData:
                                type: string
                              objectStorageScriptLocation:
                                description: DrPlanPlanGroupStepUserDefinedStepObjectStorageScriptLocation
                                  defines nested fields for DrPlan.PlanGroup.Step.UserDefinedStep.ObjectStorageScriptLocation.
                                properties:
                                  bucket:
                                    description: |-
                                      The bucket name inside the object storage namespace.
                                      Example: `custom_dr_scripts`
                                    type: string
                                  namespace:
                                    description: |-
                                      The namespace in object storage (Note - this is usually the tenancy name).
                                      Example: `myocitenancy`
                                    type: string
                                  object:
                                    description: |-
                                      The object name inside the object storage bucket.
                                      Example: `validate_app_start.sh`
                                    type: string
                                required:
                                - bucket
                                - namespace
                                - object
                                type: object
                              requestBody:
                                description: |-
                                  The request body for the function.
                                  Example: `{ "FnParam1", "FnParam2" }`
                                type: string
                              runAsUser:
                                description: |-
                                  The userid on the instance to be used for executing the script or command.
                                  Example: `opc`
                                type: string
                              runOnInstanceId:
                                description: |-
                                  The OCID of the instance on which this precheck script or command should be executed.
                                  **For moving instances:** *runOnInstanceId* must be the OCID of the instance in the region where the
                                  instance is currently present.
                                  **For non-moving instances:** *runOnInstanceId* must be the OCID of the non-moving instance.
                                  Example: `ocid1.instance.oc1..uniqueID`
                                type: string
                              scriptCommand:
                                description: |-
                                  The script name and arguments.
                                  Example: `/usr/bin/python3 /home/opc/scripts/my_app_script.py arg1 arg2 arg3`
                                type: string
                              stepType:
                                type: string
                            type: object
                        type: object
                      type: array
                    type:
                      description: |-
                        The group type.
                        Example: `BUILT_IN`
                      type: string
                  type: object
                type: array
              sourcePlanId:
                description: |-
                  The OCID of the source DR plan that should be cloned.
                  Example: `ocid1.drplan.oc1..uniqueID`
                type: string
              type:
                description: The type of DR plan to be created.
                type: string
            required:
            - displayName
            - drProtectionGroupId
            - type
            type: object
          status:
            description: DrPlanStatus defines the observed state of DrPlan.
            properties:
              compartmentId:
                description: |-
                  The OCID of the compartment containing the DR plan.
                  Example: `ocid1.compartment.oc1..uniqueID`
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Defined tags for this resource. Each key is predefined and scoped to a namespace.
                  Example: `{"Operations": {"CostCenter": "42"}}`
                type: object
              displayName:
                description: |-
                  The display name of the DR plan.
                  Example: `EBS Switchover PHX to IAD`
                type: string
              drProtectionGroupId:
                description: |-
                  The OCID of the DR protection group to which this DR plan belongs.
                  Example: `ocid1.drplan.oc1..uniqueID`
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                description: |-
                  Simple key-value pair that is applied without any predefined name, type or scope. Exists for cross-compatibility only.
                  Example: `{"Department": "Finance"}`
                type: object
              id:
                description: |-
                  The OCID of the DR plan.
                  Example: `ocid1.drplan.oc1..uniqueID`
                type: string
              lifeCycleDetails:
                description: A message describing the DR plan's current state in more
                  detail.
                type: string
              lifecycleState:
                description: The current state of the DR plan.
                type: string
              lifecycleSubState:
                description: The current state of the DR plan.
                type: string
              peerDrProtectionGroupId:
                description: |-
                  The OCID of the peer DR protection group associated with this plan's
                  DR protection group.
                  Example: `ocid1.drprotectiongroup.oc1..uniqueID`
                type: string
              peerRegion:
                description: |-
                  The region of the peer DR protection group associated with this plan's
                  DR protection group.
                  Example: `us-ashburn-1`
                type: string
              planGroups:
                description: The list of groups in this DR plan.
                items:
                  description: DrPlanPlanGroup defines nested fields for DrPlan.PlanGroup.
                  properties:
                    displayName:
                      description: |-
                        The display name of the group.
                        Example: `My_GROUP_3 - EBS Start`
                      type: string
                    id:
                      description: |-
                        The unique id of the group. Must not be modified by user.
                        Example: `sgid1.group..uniqueID`
                      type: string
                    isPauseEnabled:
                      description: |-
                        A flag indicating whether this group should be enabled for execution.
                        This flag is only applicable to the `USER_DEFINED_PAUSE` group. The flag should be null for the remaining group types.
                        Example: `true`
                      type: boolean
                    steps:
                      description: The list of steps in this group.
                      items:
                        description: DrPlanPlanGroupStep defines nested fields for
                          DrPlan.PlanGroup.Step.
                        properties:
                          displayName:
                            description: |-
                              The display name of the step in a group.
                              Example: `My_STEP_3A - EBS Start - STAGE A`
                            type: string
                          errorMode:
                            description: |-
                              The error mode for this step.
                              The default error mode for the step is `STOP_ON_ERROR`.
                            type: string
                          id:
                            description: |-
                              The unique id of the step.
                              Example: `sgid1.step..uniqueID`
                            type: string
                          isEnabled:
                            description: |-
                              A flag indicating whether this step should be enabled for execution.
                              The default value for the isEnabled flag is `true`.
                              Example: `true`
                            type: boolean
                          timeout:
                            description: |-
                              The timeout in seconds for executing this step.
                              When creating a new step, if no timeout is specified, the default timeout is set to `3600` seconds.
                              Example: `600`
                            type: integer
                          userDefinedStep:
                            description: DrPlanPlanGroupStepUserDefinedStep defines
                              nested fields for DrPlan.PlanGroup.Step.UserDefinedStep.
                            properties:
                              functionId:
                                description: |-
                                  The OCID of function to be invoked.
                                  Example: `ocid1.fnfunc.oc1..uniqueID`
                                type: string
                              jsonData:
                                type: string
                              objectStorageScriptLocation:
                                description: DrPlanPlanGroupStepUserDefinedStepObjectStorageScriptLocation
                                  defines nested fields for DrPlan.PlanGroup.Step.UserDefinedStep.ObjectStorageScriptLocation.
                                properties:
                                  bucket:
                                    description: |-
                                      The bucket name inside the object storage namespace.
                                      Example: `custom_dr_scripts`
                                    type: string
                                  namespace:
                                    description: |-
                                      The namespace in object storage (Note - this is usually the tenancy name).
                                      Example: `myocitenancy`
                                    type: string
                                  object:
                                    description: |-
                                      The object name inside the object storage bucket.
                                      Example: `validate_app_start.sh`
                                    type: string
                                required:
                                - bucket
                                - namespace
                                - object
                                type: object
                              requestBody:
                                description: |-
                                  The request body for the function.
                                  Example: `{ "FnParam1", "FnParam2" }`
                                type: string
                              runAsUser:
                                description: |-
                                  The userid on the instance to be used for executing the script or command.
                                  Example: `opc`
                                type: string
                              runOnInstanceId:
                                description: |-
                                  The OCID of the instance on which this precheck script or command should be executed.
                                  **For moving instances:** *runOnInstanceId* must be the OCID of the instance in the region where the
                                  instance is currently present.
                                  **For non-moving instances:** *runOnInstanceId* must be the OCID of the non-moving instance.
                                  Example: `ocid1.instance.oc1..uniqueID`
                                type: string
                              scriptCommand:
                                description: |-
                                  The script name and arguments.
                                  Example: `/usr/bin/python3 /home/opc/scripts/my_app_script.py arg1 arg2 arg3`
                                type: string
                              stepType:
                                type: string
                            type: object
                        type: object
                      type: array
                    type:
                      description: |-
                        The group type.
                        Example: `BUILT_IN`
                      type: string
                  type: object
                type: array
              sourcePlanId:
                description: |-
                  If this is a cloned DR plan, the OCID of the source DR plan that was used to clone this DR plan.
                  If this DR plan was not cloned, then the value for this will be `null`.
                  Example: `ocid1.drplan.oc1..uniqueID`
                type: string
              status:
                properties:
                  async:
                    description: |-
                      Async is the canonical controller-owned async contract. Resource-local
                      legacy work-request fields may remain as compatibility mirrors while
                      follow-on migrations land, but new async state should project here first.
                    properties:
                      current:
                        properties:
                          message:
                            type: string
                          normalizedClass:
                            enum:
                            - pending
                            - succeeded
                            - failed
                            - canceled
                            - attention
                            - unknown
                            type: string
                          percentComplete:
                            maximum: 100
                            minimum: 0
                            type: number
                          phase:
                            enum:
                            - create
                            - update
                            - delete
                            type: string
                          rawOperationType:
                            type: string
                          rawStatus:
                            type: string
                          source:
                            enum:
                            - none
                            - lifecycle
                            - workrequest
                            type: string
                          updatedAt:
                            format: date-time
                            type: string
                          workRequestId:
                            type: string
                        required:
                        - normalizedClass
                        - phase
                        - source
                        - updatedAt
                        type: object
                    type: object
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  opcRequestId:
                    description: |-
                      OpcRequestID is the latest non-empty OCI request ID from a mutating OCI
                      response or surfaced OCI service error that materially contributed to the
                      current shared status projection. Headerless follow-up observations keep
                      the last non-empty value intact.
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
              systemTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                description: |-
                  Usage of system tag keys. These predefined keys are scoped to namespaces.
                  Example: `{"orcl-cloud": {"free-tier-retained": "true"}}`
                type: object
              timeCreated:
                description: |-
                  The date and time the DR plan was created. An RFC3339 formatted datetime string.
                  Example: `2019-03-29T09:36:42Z`
                type: string
              timeUpdated:
                description: |-
                  The date and time the DR plan was updated. An RFC3339 formatted datetime string.
                  Example: `2019-03-29T09:36:42Z`
                type: string
              type:
                description: The type of the DR plan.
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
